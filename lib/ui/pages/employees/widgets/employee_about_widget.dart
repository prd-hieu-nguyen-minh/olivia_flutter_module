import 'package:flutter/material.dart';

class EmployeeAboutWidget extends StatelessWidget {
  const EmployeeAboutWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("About widget"),
    );
  }
}
