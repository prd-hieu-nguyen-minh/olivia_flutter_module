import 'package:flutter/material.dart';

class EmployeeConversationWidget extends StatelessWidget {
  const EmployeeConversationWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Conversation widget"),
    );
  }
}
